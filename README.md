# This project is about the how to create the Forms Based on the Models.
# models.py

First of all we have to intialize the models.py what ever fileds we want in the our database.
After declare the models.py we have to make the migrtions for our app becuase then our all data is ready to create tables in our database.
After we migrate the models the it commit all the data into database.

## forms.py

This is the file we have to create and based on this file  we have to make import the model for which model we have to create forms.
Based on this one we can add to the fileds for our own that is very simple to do it.

## views.py 


Here we will write the business logic means where we have to stroe data and which folders have to redirect all depends on the here 
business logic only it is main thing for the run application.
